       IDENTIFICATION DIVISION.
       PROGRAM-ID. ENV-VARS-EXAMPLE.
       
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 WS-RETURN-CODE PIC S9(4) COMP-5.
       01 WS-CURRENT-DATE PIC X(8).
       01 WS-CURRENT-TIME PIC X(8).
       01 WS-COMPILED-DATE PIC X(8).
       01 WS-COMPILED-TIME PIC X(8).
       01 WS-PATH-VAR PIC X(255).
       
       PROCEDURE DIVISION.
       MAIN-LOGIC.
           DISPLAY "--- COBOL Environment Variables and Constants ---"
           
           *>--- Special Registers ---*
           
           *> Displaying RETURN-CODE
           MOVE 0 TO RETURN-CODE
           MOVE RETURN-CODE TO WS-RETURN-CODE
           DISPLAY "RETURN-CODE initialized to: " WS-RETURN-CODE
           
           *> Displaying DATE and TIME Special Registers
           ACCEPT WS-CURRENT-DATE FROM DATE
           ACCEPT WS-CURRENT-TIME FROM TIME
           DISPLAY "CURRENT-DATE: " WS-CURRENT-DATE
           DISPLAY "CURRENT-TIME: " WS-CURRENT-TIME
           
           *> Displaying WHEN-COMPILED
           MOVE WHEN-COMPILED(1:8) TO WS-COMPILED-DATE
           MOVE WHEN-COMPILED(9:8) TO WS-COMPILED-TIME
           DISPLAY "WHEN-COMPILED (Date): " WS-COMPILED-DATE
           DISPLAY "WHEN-COMPILED (Time): " WS-COMPILED-TIME
           
        *>    *--- Figurative Constants ---*
           
           DISPLAY " "
           DISPLAY "--- Figurative Constants ---"
           
        *>    * Displaying ZEROS
           DISPLAY "ZEROS: " ZEROS
           
        *>    * Displaying SPACES
           DISPLAY "SPACES (enclosed in quotes): '" SPACES "'"
           
        *>    * Displaying HIGH-VALUES
           DISPLAY "HIGH-VALUES: " HIGH-VALUES
           
        *>    * Displaying LOW-VALUES
           DISPLAY "LOW-VALUES: " LOW-VALUES
           
        *>    *--- Environment Variables ---*
           
           DISPLAY " "
           DISPLAY "--- Operating System Environment Variables ---"
           
        *>    * This part may not work on all compilers. GnuCOBOL is a good choice.
           ACCEPT WS-PATH-VAR FROM ENVIRONMENT "PATH"
           DISPLAY "PATH environment variable: " WS-PATH-VAR
           
           GOBACK.